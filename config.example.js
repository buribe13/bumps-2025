// ⚠️ API Configuration Template ⚠️
//
// SETUP INSTRUCTIONS:
// 1. Copy this file to config.js: cp config.example.js config.js
// 2. Get your API key from: https://platform.openai.com/api-keys
// 3. Open config.js and replace "YOUR_OPENAI_API_KEY_HERE" with your actual key
// 4. Save config.js - it is gitignored and will NOT be committed
//
// SECURITY WARNINGS:
// - config.js is gitignored - DO NOT remove it from .gitignore
// - NEVER commit config.js to version control
// - NEVER share your API key publicly or in screenshots
// - If your key is exposed, revoke it immediately and generate a new one
// - This example file is safe to commit (it contains no real keys)
window.OPENAI_API_KEY = "YOUR_OPENAI_API_KEY_HERE";

