# Copy to .env.local for local development
OPENAI_API_KEY=your_openai_api_key_here

# Optional — defaults to the value in lib/bumps-app.js if unset
NEXT_PUBLIC_SPOTIFY_CLIENT_ID=your_spotify_client_id_here
